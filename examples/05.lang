struct Rect {
    width: number;
    static heigth: number;
}

const square = Rect{
    width: 10,
    heigth: 10
};


let foo = 45.5;
let bar = foo * 10;
const isGreater = foo > 10;

// add support for types
let number: []numbers;

const bar: [][]number = 10;
const numbers = []number{1, 2};
